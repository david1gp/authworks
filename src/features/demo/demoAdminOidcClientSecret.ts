/**
 * A deterministic, clearly fake client secret used only by the one-time demo state.
 * It is not a credential and never authenticates against any Authworks realm.
 */
export const demoAdminOidcClientSecret = "demo-secret-0000example00000not00000a00000real00000credential"
