/**
 * A deterministic, clearly fake credential value used only by the URL-selectable one-time
 * demo state. It is long enough to satisfy the public issue contract and is never usable.
 */
export const demoAdminMachineSecret = "demo-machine-secret-000000000000000000000000"
