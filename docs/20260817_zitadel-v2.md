# ZITADEL v2 backend

## Goal

Build `/home/david/adaptive/zitadel-v2` as an alpha, backend-first identity platform covering all agreed capabilities from levels 1 through 6. Deliver a usable server, a public schema/API-client library, and an `@stricli/core` CLI. Preserve observable behavior from existing ZITADEL and adaptive authentication tests where applicable, with correctness and security tests taking priority over implementation reuse.

## Decisions

- Use Bun, strict ESM TypeScript, Hono, Valibot, Drizzle ORM, SQLite, Biome, and Bun tests, following `/home/david/adaptive/codeline` conventions where they apply.
- Use one Bun package organized feature-first. Each `src/features/<feature>` directory owns its domain, actions, state persistence, event payloads, server routes, public schemas, API client, CLI commands, tests, and future UI.
- Keep `src/outputs/server.ts`, `src/outputs/library.ts`, and `src/outputs/cli.ts` as thin static compositions built to `dist/{server,library,cli}`. They contain no feature behavior.
- Keep public Valibot transport schemas separate from private Drizzle persistence schemas. Share explicit domain/public schemas where safe; never derive public contracts blindly from database rows.
- Perform backend, library, and CLI work before creating any UI.
- Use canonical lowercase UUIDv7 strings for public durable resource and event IDs. Use random opaque secrets for credentials, sessions, authorization codes, refresh tokens, API keys, challenges, nonce, and state. Use an internal integer event position for strict ordering.
- Use one file-backed SQLite database, `zitadel.sqlite`, for current state and the append-only event log. Configure `journal_mode=WAL`, `synchronous=FULL`, `foreign_keys=ON`, `temp_store=MEMORY`, and `busy_timeout=5000`, adapting the proven `/home/david/leo_own/gruppenplan-app` presets for identity-data durability. Write state plus events in one transaction.
- Current-state tables are authoritative. The append-only event table records versioned domain facts with event ID, position, command index, instance, aggregate identity/version, actor, correlation/causation, timestamp, payload, and metadata. Never record secrets or credential material.
- Alpha rule: destructive schema changes and database resets are allowed. Do not implement backward compatibility, compatibility adapters, event upcasters, deprecated contracts, or migration paths for existing data unless explicitly requested. Keep only what is needed to construct and test the current schema from an empty database.
- Before implementing a feature, inspect existing ZITADEL, `adaptive/convex-auth-solid`, `leo/allgroups-chat`, and `adaptive/zitadel-login` behavior and tests as applicable. Port observable behavior, not framework-specific implementation.
- Tests are more important than implementation. Cover positive behavior, negative security behavior, tenant isolation, transaction/event atomicity, public contracts, clients, and CLI behavior. Keep the branch green; do not commit intentionally failing tests.
- Each completed feature is a vertically complete commit including behavior tests, implementation, events, public schemas, API client, and CLI where applicable. Use the `commits` skill for every feature commit and follow the `code-style` skill wherever applicable.
- Parallelize only after shared contracts are accepted. Serialize storage/migration integration, session assurance, OIDC security boundaries, key management, and final composition.
- Level 1 includes users, password registration/login, verification, recovery, sessions, revocation, rate limiting, protected APIs, and profile lifecycle.
- Level 2 includes email OTP, Google/GitHub/Microsoft login, recent accounts, multiple sessions, email/password changes, and account deletion.
- Level 3 includes instances, organizations, memberships, invitations, organization switching, roles, permissions, and tenant isolation.
- Level 4 includes OIDC clients, exact redirect validation, authorization code with PKCE, access/ID/refresh tokens, discovery, JWKS, UserInfo, scopes/claims, consent/trusted clients, client secrets, revocation, and logout.
- Level 5 includes TOTP, recovery codes, passkeys/WebAuthn, MFA enrollment/removal/enforcement, lockout, security notifications, device/session management, and step-up assurance.
- Level 6 includes multiple organizations per instance, organization branding and identity-provider configuration, account linking, domain discovery, machine users, personal access tokens, client credentials, projects/applications/roles, cross-organization grants, fine-grained policy evaluation, and admin impersonation.

## Approach

- Establish shared tooling, import boundaries, dependency rules, injectable runtime seams, WAL SQLite storage, event transactions, testkit, and thin output composition first.
- Implement tenancy and identity foundations before authentication methods. Define session assurance, actor context, authorization errors, and event conventions before OIDC or MFA.
- Implement each capability as a removable feature directory with tests beside the feature. Features expose explicit `public`, `client`, `server`, `cli`, and future `ui` modules only when those surfaces exist.
- Use static output composition so missing feature imports fail at build time. Define library subpath exports and the CLI binary in the root `package.json`. The CLI calls the server through feature API clients and never accesses SQLite directly.
- Generate and reset the database from the current schema. Test with real temporary file-backed databases, real cryptography where relevant, and one isolated database file per parallel test.
- Implement external network operations outside database transactions. Persist commands and events synchronously, then perform notifications or provider calls after commit through explicit ports.

## Tasks

- [x] 1. Scaffold the single package, repository instructions, strict tooling, import boundaries, thin server/library/CLI entries and builds, and baseline test commands.
- [x] 2. Implement platform primitives: UUIDv7, clocks/randomness, errors/results, secret handling, HTTP conventions, configuration, and testkit.
- [x] 3. Implement single-file WAL SQLite storage, Drizzle schema composition/reset, transactional event append, append-only enforcement, PRAGMA verification, and crash/rollback-oriented integration tests.
- [x] 4. Implement instances and bootstrap administration with strict tenant context and isolation tests.
- [x] 5. Implement organizations, memberships, invitations, switching, lifecycle, and organization roles.
- [x] 6. Implement users, profiles, lifecycle states, verification state, account deletion, and audit-safe events.
- [x] 7. Implement authorization foundations: actor context, fixed/custom roles, permissions, policy evaluation, and tenant-aware enforcement.
- [x] 8. Implement password credentials, registration/login, password policy, lockout, verification, recovery, changes, and enumeration resistance.
- [x] 9. Implement sessions, rotation, multiple/recent sessions, device metadata, revocation, assurance, and protected API middleware.
- [x] 10. Implement email OTP authentication, cooldown/replay protection, delivery ports, and security notifications.
- [x] 11. Implement external identities and safe account linking, then Google, GitHub, and Microsoft providers with instance/organization configuration.
- [x] 12. Implement projects, applications, application roles, grants, and cross-organization grants.
- [x] 13. Implement OIDC client management, signing keys/JWKS, discovery, authorization-code PKCE, tokens, UserInfo, scopes/claims, consent, revocation, and logout with protocol/security tests.
- [x] 14. Implement TOTP, recovery codes, MFA policy/enrollment/removal, step-up authentication, and assurance claims.
- [x] 15. Implement passkeys/WebAuthn for passwordless and MFA use, including discoverable credentials and protocol security tests.
- [ ] 16. Implement machine users, client credentials, personal access tokens, API keys, rotation, and revocation.
- [ ] 17. Implement organization branding, domain discovery, and organization-specific login/provider policy.
- [ ] 18. Implement admin impersonation with explicit authorization, session marking, limits, and audit events.
- [ ] 19. Complete public library feature subpaths, all API clients, and all `@stricli/core` command trees against the running server.
- [ ] 20. Run full cross-feature conformance, isolation, event atomicity, API-client, CLI subprocess, and security regression suites; verify all three distributable outputs from a clean checkout.

## Paths

- Plan: `/home/david/adaptive/zitadel-v2/docs/20260817_zitadel-v2.md`
- Target: `/home/david/adaptive/zitadel-v2`
- Source: `/home/david/adaptive/zitadel-v2/src`
- Features: `/home/david/adaptive/zitadel-v2/src/features/*`
- Platform: `/home/david/adaptive/zitadel-v2/src/platform/*`
- Output entries: `/home/david/adaptive/zitadel-v2/src/outputs/{server,library,cli}.ts`
- Builds: `/home/david/adaptive/zitadel-v2/dist/{server,library,cli}`
- Primary reference: `/home/david/opensource/zitadel`
- Stack reference: `/home/david/adaptive/codeline`
- Authentication reference: `/home/david/adaptive/convex-auth-solid`
- Workspace/authorization reference: `/home/david/leo/allgroups-chat`
- Login/security reference: `/home/david/adaptive/zitadel-login`
- SQLite preset reference: `/home/david/leo_own/gruppenplan-app`
